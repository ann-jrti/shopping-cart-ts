import { Button } from '@mui/material';
import { CartItemType } from '../../types/CartItemType';
import { WrapperCartItem } from './CartItem.style';

const CartItem: React.FC = () => <div>Cart Item</div>;

export default CartItem;
